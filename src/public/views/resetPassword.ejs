<!DOCTYPE html>
<html lang="ar" dir="rtl" data-theme="black">

<%- include("./partials/header.ejs", { pageTitle: 'إعادة تعيين كلمة المرور' }) %>

<body class="min-h-screen bg-black">
     <%- include("./partials/navbar.ejs") %>

     <div class="min-h-screen flex items-center justify-center px-4 pb-24 pt-10 md:pt-24 md:pb-10">
          <div class="w-full max-w-md">
               <div class="card bg-blur border border-neutral-600 shadow-xl">
                    <div class="card-body gap-5">
                         <div class="text-center">
                              <h1 class="text-xl font-bold text-white">إعادة تعيين كلمة المرور</h1>
                              <div class="w-16 border-b radial-border mx-auto mt-3"></div>
                         </div>

                         <% if (typeof errors !== 'undefined' && errors) { %>
                              <div class="alert alert-soft alert-error text-sm" role="alert">
                                   <ul class="list-disc ps-4">
                                        <% errors.forEach(e => { %><li><%= e.msg %></li><% }) %>
                                   </ul>
                              </div>
                         <% } %>
                         <% if (typeof error !== 'undefined' && error) { %>
                              <div class="alert alert-soft alert-error text-sm" role="alert"><%= error %></div>
                         <% } %>
                         <% if (typeof note !== 'undefined' && note) { %>
                              <div class="alert alert-soft alert-success text-sm" role="alert"><%= note %></div>
                         <% } %>

                         <% if (typeof note === 'undefined' || !note) { %>
                              <form method="POST" action="/reset-password/<%= email %>/<%= otp %>" class="flex flex-col gap-4">
                                   <div class="input-floating">
                                        <input type="password" name="password" id="newPassword" placeholder="••••••••"
                                               class="input" required minlength="8" maxlength="128" autocomplete="new-password">
                                        <label class="input-floating-label" for="newPassword">كلمة المرور الجديدة</label>
                                   </div>

                                   <button type="submit" class="btn btn-primary w-full mt-1">
                                        تأكيد كلمة المرور الجديدة
                                        <i class="fi fi-sr-check text-sm"></i>
                                   </button>
                              </form>
                         <% } else { %>
                              <a href="/login" class="btn btn-primary w-full">تسجيل الدخول بكلمة المرور الجديدة</a>
                         <% } %>
                    </div>
               </div>
          </div>
     </div>
</body>
</html>
